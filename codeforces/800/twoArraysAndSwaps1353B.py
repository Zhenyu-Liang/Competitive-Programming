_=input
a=lambda:[*map(int,input().split())]
for i in range(int(_())):
    z,x,y=[a()for i in" "*3]
    z=z[1]
    y=[i for i in y if i>min(x)]
    if len(y)<1:0
    else:
        for j in" "*z:
            x.remove(min(x))
            x+=[max(y)]
            y.remove(max(y))
            if len(y)==0 or max(y)<=min(x):break
    print(sum(x))
        
        
